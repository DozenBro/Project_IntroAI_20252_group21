data:
  raw_path: data/raw/StudentPerformanceFactors.csv
  db_path: data/database/smartedu.db
  target_col: Exam_Score

preprocessing:
  test_size: 0.2
  random_state: 42
  cv_folds: 5

model:
  xgboost:
    n_estimators: 100
    max_depth: 6
    learning_rate: 0.1